---
phase: 01-core-backend-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/nest-cli.json
  - backend/.env.example
  - backend/.gitignore
  - backend/railway.toml
  - backend/prisma/schema.prisma
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/config/env.validation.ts
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/health/health.controller.ts
  - backend/src/health/health.controller.spec.ts
  - backend/test/app.e2e-spec.ts
  - .env.example
autonomous: false
requirements: []
user_setup:
  - service: railway-postgres
    why: "Prisma migrations require a reachable PostgreSQL instance; Railway PostgreSQL add-on (or local docker postgres:16) provides DATABASE_URL"
    env_vars:
      - name: DATABASE_URL
        source: "Railway Dashboard -> PostgreSQL service -> Connect -> Postgres Connection URL (append ?sslmode=require for Railway)"
    dashboard_config:
      - task: "Provision PostgreSQL add-on and set backend service root directory to /backend"
        location: "Railway Dashboard -> New -> Database -> PostgreSQL; backend service -> Settings -> Root Directory = backend"
      - task: "Set the railway.toml config path to /backend/railway.toml"
        location: "Railway Dashboard -> backend service -> Settings -> Config-as-code -> /backend/railway.toml"

must_haves:
  truths:
    - "The NestJS backend boots locally via `node dist/main.js` and serves GET /health returning { status: 'ok' }"
    - "`npx prisma migrate dev` creates the generation_requests and generation_results tables in PostgreSQL"
    - "Boot fails fast with a descriptive error if a required env var (DATABASE_URL, REDIS_URL, OPENAI_API_KEY) is missing"
    - "Express trusts the first proxy hop so req.ip reflects X-Forwarded-For"
    - "railway.toml runs `npx prisma migrate deploy` before `node dist/main.js` at start"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Prisma 7 generator (provider=prisma-client, output=../src/generated/prisma) + GenerationRequest/GenerationResult models"
      contains: "model GenerationRequest"
    - path: "backend/src/prisma/prisma.service.ts"
      provides: "PrismaService extending generated PrismaClient with lifecycle hooks"
      min_lines: 10
    - path: "backend/src/config/env.validation.ts"
      provides: "Zod EnvSchema + validate() for ConfigModule fail-fast validation"
      exports: ["validate", "EnvConfig"]
    - path: "backend/src/main.ts"
      provides: "Bootstrap: trust proxy, CORS, ValidationPipe, shutdown hooks, listen on PORT"
      contains: "trust proxy"
    - path: "backend/railway.toml"
      provides: "Railway build + deploy config with migrate deploy in start command"
      contains: "migrate deploy"
    - path: "backend/src/health/health.controller.ts"
      provides: "GET /health walking-skeleton probe"
      contains: "@Controller('health')"
  key_links:
    - from: "backend/src/app.module.ts"
      to: "backend/src/config/env.validation.ts"
      via: "ConfigModule.forRoot({ isGlobal: true, validate })"
      pattern: "validate"
    - from: "backend/src/prisma/prisma.service.ts"
      to: "backend/src/generated/prisma"
      via: "import PrismaClient from generated output path"
      pattern: "generated/prisma"
    - from: "backend/src/main.ts"
      to: "express trust proxy"
      via: "app.getHttpAdapter().getInstance().set('trust proxy', 1)"
      pattern: "trust proxy"
---

<objective>
Bootstrap the NestJS backend as a Walking Skeleton: scaffold the monorepo `backend/` project, wire fail-fast Zod env validation, define the Prisma 7 schema, run the first migration against PostgreSQL, expose a GET /health probe, and produce a Railway deployment config that migrates at start. This is the thinnest possible end-to-end slice proving every infrastructure layer connects (scaffold → config → DB schema → migration → HTTP route → deploy config).

Purpose: Prove the project boots and reaches the database before any LLM, cache, or rate-limit feature work begins. All downstream plans (01-02, 01-03) and all of Phase 2 build on the modules and PrismaService established here.

Output: A runnable NestJS backend with a migrated PostgreSQL schema, a working /health endpoint, fail-fast config validation, Railway config, and SKELETON.md documenting the end-to-end proof.

Implements D-01, D-02, D-03 (project structure), D-10, D-11 (Prisma schema), D-16 (trust proxy), D-19, D-20, D-21 (Railway config), D-22 (ConfigModule + Zod env validation). Honors Claude's Discretion on manual PrismaService (chosen over nestjs-prisma per RESEARCH Open Question 1 — zero extra dependency, verified Prisma 7 compatible) and CORS (FRONTEND_URL in prod, all origins in dev).
</objective>

## Phase Goal

**As an** API consumer of the Melotech Metagen backend, **I want to** reach a deployed NestJS service whose database schema is migrated and whose configuration is validated at boot, **so that** every later capability (LLM generation, caching, rate limiting) has a proven, deployable foundation to build on.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-backend-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-backend-infrastructure/01-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan (01-01) creates the following symbols and files. Downstream plans and phases consume these:

**Files / directories:**
- `backend/` — monorepo backend service root (D-01, D-03)
- `backend/package.json`, `backend/tsconfig.json`, `backend/nest-cli.json` — NestJS project files (D-02)
- `backend/.env.example`, `.env.example` (root) — documented env vars (D-02)
- `backend/.gitignore` — includes `src/generated/` (RESEARCH Open Question 2)
- `backend/railway.toml` — Railway build/deploy config (D-21)
- `backend/prisma/schema.prisma` — Prisma 7 schema (D-10, D-11)
- `backend/prisma/migrations/` — first migration directory
- `backend/src/generated/prisma/` — generated Prisma Client (gitignored)

**Classes / providers:**
- `PrismaService` (backend/src/prisma/prisma.service.ts) — singleton extending generated `PrismaClient`
- `PrismaModule` (backend/src/prisma/prisma.module.ts) — `@Global()` module exporting `PrismaService`
- `AppModule` (backend/src/app.module.ts) — root module
- `HealthController` (backend/src/health/health.controller.ts) — GET /health

**Functions / types:**
- `validate(config)` and `EnvConfig` type (backend/src/config/env.validation.ts)

**Env vars introduced:** `DATABASE_URL`, `REDIS_URL`, `OPENAI_API_KEY`, `OPENAI_MODEL`, `PORT`, `NODE_ENV`, `FRONTEND_URL` (REDIS_URL/OPENAI_* are declared here in EnvSchema so boot validation passes once 01-02/01-03 consume them).

**Prisma models (DB contract for Phase 2):** `GenerationRequest` (generation_requests), `GenerationResult` (generation_results).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold NestJS backend, install dependencies, wire fail-fast Zod env validation</name>
  <read_first>
    - .planning/phases/01-core-backend-infrastructure/01-RESEARCH.md (Standard Stack table for exact versions; Pattern 1 main.ts; Pattern 8 ConfigModule + Zod env validation; Installation section; Environment Availability table)
    - .planning/phases/01-core-backend-infrastructure/01-CONTEXT.md (D-01, D-02, D-03, D-16, D-20, D-22)
  </read_first>
  <files>backend/package.json, backend/tsconfig.json, backend/nest-cli.json, backend/.env.example, backend/.gitignore, .env.example, backend/src/main.ts, backend/src/app.module.ts, backend/src/config/env.validation.ts</files>
  <action>
    Scaffold the backend into `backend/` using `npx @nestjs/cli@latest new backend --package-manager npm --skip-git` (the repo root is already a project dir; do NOT init a second git repo). Per D-01/D-03 the project lives in `backend/` with its own package.json and tsconfig.json (D-02).

    Install runtime deps at the exact versions from RESEARCH Standard Stack: `@nestjs/config@4.0.4`, `zod@4.4.3`, plus `class-validator@0.15.1` and `class-transformer@0.5.1`. (Prisma and other module deps install in later tasks/plans.) Set Node engine to >=22.

    Add to `backend/package.json` scripts: `"postinstall": "prisma generate"` (D-19; harmless until prisma is added in Task 2 — but install order in Task 2 runs after prisma is present, so place this script now and ensure it does not break the initial `npm install` by also installing prisma in this task — install `prisma@7.8.0` and `@prisma/client@7.8.0` here too so postinstall has something to run).

    Create `backend/src/config/env.validation.ts` exporting a Zod `EnvSchema` and a `validate(config: Record<string, unknown>)` function exactly per RESEARCH Pattern 8: fields `DATABASE_URL` (url), `REDIS_URL` (url), `OPENAI_API_KEY` (min 1), `OPENAI_MODEL` (default 'gpt-4o-2024-08-06'), `PORT` (coerce.number default 3001), `NODE_ENV` (enum development|production|test default development), `FRONTEND_URL` (url optional). On `safeParse` failure, throw an Error listing each `path: message`. Export `EnvConfig = z.infer<typeof EnvSchema>` (D-22, D-06, D-20).

    Wire `ConfigModule.forRoot({ isGlobal: true, validate })` into `backend/src/app.module.ts`.

    Rewrite `backend/src/main.ts` per RESEARCH Pattern 1: `app.getHttpAdapter().getInstance().set('trust proxy', 1)` BEFORE listen (D-16); `app.enableCors({ origin: process.env.FRONTEND_URL ?? true, methods: ['GET','POST'], allowedHeaders: ['Content-Type'] })` (Claude's Discretion — CORS); `app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }))`; `app.enableShutdownHooks()`; `await app.listen(process.env.PORT ?? 3001)`.

    Create `backend/.env.example` and root `.env.example` (D-02) documenting all D-20 vars: `DATABASE_URL`, `REDIS_URL`, `OPENAI_API_KEY`, `OPENAI_MODEL=gpt-4o-2024-08-06`, `PORT=3001`, `NODE_ENV=production`, and `FRONTEND_URL`. Add `src/generated/` and `.env` to `backend/.gitignore` (RESEARCH Open Question 2).

    Do NOT use `provider = "prisma-client-js"` anywhere; do NOT add Joi (use Zod per RESEARCH Alternatives Considered).
  </action>
  <verify>
    <automated>cd backend && npm run build && grep -q "trust proxy" src/main.ts && grep -q "isGlobal: true" src/app.module.ts && node -e "const {validate}=require('./dist/config/env.validation.js'); try{validate({});process.exit(1)}catch(e){process.exit(0)}"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm run build` exits 0 (TypeScript compiles).
    - `backend/src/config/env.validation.ts` exports `validate` and `EnvConfig`; calling `validate({})` throws (proves fail-fast on missing required vars).
    - `grep -c "trust proxy" backend/src/main.ts` returns >= 1.
    - `backend/src/main.ts` calls `app.enableShutdownHooks()` and `app.useGlobalPipes` with a `ValidationPipe`.
    - `backend/.env.example` and root `.env.example` both list DATABASE_URL, REDIS_URL, OPENAI_API_KEY, OPENAI_MODEL, PORT, NODE_ENV (verify: `grep -c -E 'DATABASE_URL|REDIS_URL|OPENAI_API_KEY' backend/.env.example` returns 3).
    - `backend/.gitignore` contains `src/generated/` (verify: `grep -q 'src/generated' backend/.gitignore`).
    - No occurrence of `prisma-client-js` in any backend file (`grep -rL` style: `! grep -rq 'prisma-client-js' backend/src backend/prisma 2>/dev/null`).
  </acceptance_criteria>
  <done>Backend project compiles, env validation fails fast on missing vars, trust proxy + CORS + ValidationPipe + shutdown hooks wired in main.ts, env examples and gitignore in place.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma 7 schema, PrismaModule/PrismaService, HealthController, Railway config</name>
  <read_first>
    - .planning/phases/01-core-backend-infrastructure/01-RESEARCH.md (Pattern 2 Prisma 7 generator — CRITICAL; Pattern 3 PrismaService; Pattern 9 railway.toml; Walking Skeleton Health Endpoint; Anti-Patterns to Avoid; Pitfalls 1, 4, 5)
    - .planning/phases/01-core-backend-infrastructure/01-CONTEXT.md (D-10, D-11, D-19, D-21; specifics: sslmode=require)
  </read_first>
  <files>backend/prisma/schema.prisma, backend/src/prisma/prisma.module.ts, backend/src/prisma/prisma.service.ts, backend/src/health/health.controller.ts, backend/src/health/health.controller.spec.ts, backend/src/app.module.ts, backend/railway.toml</files>
  <action>
    Run `cd backend && npx prisma init --datasource-provider postgresql` if prisma/ not already present (Task 1 installed prisma). Then overwrite `backend/prisma/schema.prisma` exactly per RESEARCH Pattern 2 (D-10, D-11):
    - generator block: `provider = "prisma-client"` (NOT `prisma-client-js` — Prisma 7) with required `output = "../src/generated/prisma"`.
    - datasource db: `provider = "postgresql"`, `url = env("DATABASE_URL")`.
    - model `GenerationRequest`: `id String @id @default(cuid())`, `prompt String`, `createdAt DateTime @default(now()) @map("created_at")`, `results GenerationResult[]`, `@@map("generation_requests")`.
    - model `GenerationResult`: `id String @id @default(cuid())`, `requestId String @map("request_id")`, `platform String`, `payload Json @map("payload_json")`, `createdAt DateTime @default(now()) @map("created_at")`, `request GenerationRequest @relation(fields: [requestId], references: [id])`, `@@map("generation_results")`.

    Create `backend/src/prisma/prisma.service.ts` per RESEARCH Pattern 3: `@Injectable() class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy` importing `PrismaClient` from `'../generated/prisma'` (custom output path — NOT `@prisma/client`, Anti-Pattern). `onModuleInit` calls `this.$connect()`; `onModuleDestroy` calls `this.$disconnect()`.

    Create `backend/src/prisma/prisma.module.ts`: `@Global() @Module({ providers: [PrismaService], exports: [PrismaService] })`.

    Create `backend/src/health/health.controller.ts` per RESEARCH Walking Skeleton Health Endpoint: `@Controller('health')` with `@Get() check()` returning `{ status: 'ok', timestamp: new Date().toISOString() }`. Create `backend/src/health/health.controller.spec.ts` — a unit test asserting `check().status === 'ok'`.

    Register `PrismaModule` and `HealthController` in `backend/src/app.module.ts` (controllers: [HealthController], imports include PrismaModule).

    Create `backend/railway.toml` per RESEARCH Pattern 9 (D-19, D-21): `[build] buildCommand = "npm run build"`; `[deploy] startCommand = "npx prisma migrate deploy && node dist/main.js"`, `healthcheckPath = "/health"`, `healthcheckTimeout = 30`, `restartPolicyType = "ALWAYS"`. Note in a comment that env vars (D-20) are set in the Railway dashboard and that `prisma generate` runs via postinstall.

    Run `cd backend && npx prisma generate` to produce the client at the custom output path so the build resolves the import.
  </action>
  <verify>
    <automated>cd backend && npx prisma generate && npx prisma validate && npm run build && grep -q 'provider = "prisma-client"' prisma/schema.prisma && grep -q 'output' prisma/schema.prisma && grep -q "migrate deploy" railway.toml && ! grep -q "prisma-client-js" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exits 0 (schema is valid).
    - `backend/prisma/schema.prisma` contains `provider = "prisma-client"` AND an `output` line AND `model GenerationRequest` AND `model GenerationResult` AND both `@@map("generation_requests")` / `@@map("generation_results")`.
    - `backend/prisma/schema.prisma` does NOT contain `prisma-client-js`.
    - `backend/src/prisma/prisma.service.ts` imports `PrismaClient` from `'../generated/prisma'` (verify: `grep -q "from '../generated/prisma'" backend/src/prisma/prisma.service.ts`) and does NOT import from `@prisma/client`.
    - `backend/railway.toml` contains `npx prisma migrate deploy && node dist/main.js` and `healthcheckPath = "/health"`.
    - `cd backend && npm run build` exits 0 after `npx prisma generate`.
    - `backend/src/health/health.controller.ts` contains `@Controller('health')`.
  </acceptance_criteria>
  <done>Prisma 7 schema valid with correct provider/output and both models; PrismaService imports from generated path; HealthController and railway.toml created; project builds with generated client.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Run first Prisma migration to create tables</name>
  <read_first>
    - .planning/phases/01-core-backend-infrastructure/01-RESEARCH.md (Pitfall 5 migrate deploy vs migrate dev; Environment Availability — PostgreSQL fallback via docker)
    - backend/prisma/schema.prisma (from Task 2)
  </read_first>
  <files>backend/prisma/migrations/</files>
  <action>
    This task is MANDATORY and BLOCKING: the phase cannot pass verification without the schema being applied to a real PostgreSQL instance. It runs AFTER Task 2 (all schema files complete) and BEFORE verification.

    Ensure a reachable PostgreSQL exists and `DATABASE_URL` is set in `backend/.env` (Railway PostgreSQL add-on per user_setup, or local `docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres:16` with `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/metagen`).

    Run `cd backend && npx prisma migrate dev --name init` to create the first migration in `backend/prisma/migrations/` AND apply it (creates `generation_requests` and `generation_results` tables). For production/Railway the start command uses `npx prisma migrate deploy` (already in railway.toml from Task 2) — do NOT run `migrate dev` in production (Anti-Pattern / Pitfall 5).

    If `DATABASE_URL` is unreachable, STOP and surface the blocking checkpoint: the developer must provision PostgreSQL and provide `DATABASE_URL` before this task can complete.
  </action>
  <verify>
    <automated>cd backend && test -d prisma/migrations && ls prisma/migrations | grep -q init && npx prisma migrate status 2>&1 | grep -qiE "up to date|following migration|applied"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/prisma/migrations/` exists and contains a migration directory matching `*init*` (verify: `ls backend/prisma/migrations | grep -q init`).
    - The migration SQL creates tables `generation_requests` and `generation_results` (verify: `grep -rqi 'generation_requests' backend/prisma/migrations && grep -rqi 'generation_results' backend/prisma/migrations`).
    - `npx prisma migrate status` reports the migration as applied / database up to date.
    - Querying the DB confirms both tables exist (verify: `cd backend && node -e "const {PrismaClient}=require('./src/generated/prisma'); const p=new PrismaClient(); p.\$queryRawUnsafe(\"SELECT to_regclass('public.generation_requests') a, to_regclass('public.generation_results') b\").then(r=>{if(r[0].a&&r[0].b){console.log('ok');process.exit(0)}process.exit(1)}).catch(()=>process.exit(1))"`).
  </acceptance_criteria>
  <done>First migration exists in prisma/migrations and is applied; generation_requests and generation_results tables exist in PostgreSQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway load balancer → Express | X-Forwarded-For header crosses here; trust only the first hop (D-16) |
| Environment → process | Secrets (DATABASE_URL, OPENAI_API_KEY) injected via env; never in code or logs |
| Express → PostgreSQL | Prisma queries cross here over TLS (sslmode=require) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | DATABASE_URL / secrets | mitigate | Env-only via @nestjs/config; Zod EnvSchema rejects missing/empty at boot; `.env` in .gitignore; never logged (ASVS V14) |
| T-01-02 | Spoofing | X-Forwarded-For IP | mitigate | `trust proxy: 1` trusts only Railway's first hop; downstream rate limiting (01-03) depends on this (ASVS V4) |
| T-01-03 | Information Disclosure | PostgreSQL connection | mitigate | `?sslmode=require` on DATABASE_URL (CONTEXT specifics); Railway PostgreSQL TLS by default (ASVS V9) |
| T-01-04 | Tampering | npm package installs | mitigate | All packages [Approved] in RESEARCH Package Legitimacy Audit (npm registry, manually verified); no [ASSUMED]/[SUS] packages require a checkpoint |
| T-01-SC | Tampering | npm/prisma installs | mitigate | Audit table present in RESEARCH; postinstall is `prisma generate` only (Prisma is Approved, no network-calling third-party postinstall) |
</threat_model>

<verification>
- `cd backend && npm run build` compiles cleanly.
- `cd backend && npx prisma validate` passes; `npx prisma migrate status` reports applied.
- Boot smoke test: with valid env, `node dist/main.js` starts and `curl -s localhost:3001/health` returns `{"status":"ok",...}` (run as part of integration verification at phase end).
- Fail-fast test: removing DATABASE_URL from env causes boot to throw the env validation error.
</verification>

<success_criteria>
- NestJS backend scaffolded in `backend/` with independent package.json/tsconfig (D-01, D-02, D-03).
- ConfigModule validates env via Zod and fails fast on missing required vars (D-22).
- Prisma 7 schema with `provider = "prisma-client"` + required output and both models (D-10, D-11).
- First migration applied; generation_requests and generation_results tables exist in PostgreSQL (Phase 1 success criterion #4).
- GET /health returns 200 (walking skeleton probe).
- main.ts sets trust proxy before listen (D-16, Phase 1 success criterion #5).
- railway.toml runs migrate deploy at start (D-19, D-21).
</success_criteria>

<output>
Create `.planning/phases/01-core-backend-infrastructure/01-01-SUMMARY.md` when done.
</output>
