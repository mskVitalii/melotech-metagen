---
phase: 01-core-backend-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/package.json
  - backend/src/cache/cache.module.ts
  - backend/src/throttler/throttler.module.ts
  - backend/src/throttler/throttler-exception.filter.ts
  - backend/src/throttler/throttler-exception.filter.spec.ts
  - backend/src/health/rate-probe.controller.ts
  - backend/test/throttler.e2e-spec.ts
  - backend/src/main.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - API-04
  - RATE-01
  - RATE-02
must_haves:
  truths:
    - "A 4th request from the same IP within 60s receives HTTP 429; the first 3 pass"
    - "The 429 response body is exactly { statusCode: 429, message: 'Too Many Requests', retryAfter: 60 } with a Retry-After: 60 header"
    - "Rate limit counters are stored in Redis (via ioredis) so they survive a server restart"
    - "getTracker extracts the real client IP (req.ips[0] ?? req.ip) so per-IP limiting works behind Railway's proxy"
    - "The cache uses a two-store setup (L1 in-memory + L2 Redis via @keyv/redis) and is globally available"
  artifacts:
    - path: "backend/src/throttler/throttler.module.ts"
      provides: "ThrottlerModule.forRoot with { ttl:60000, limit:3 }, Redis storage, inline getTracker"
      contains: "getTracker"
    - path: "backend/src/throttler/throttler-exception.filter.ts"
      provides: "ThrottlerExceptionFilter returning exact D-18 429 body + Retry-After header"
      contains: "retryAfter: 60"
    - path: "backend/src/cache/cache.module.ts"
      provides: "Global CacheModule with L1 KeyvCacheableMemory + L2 KeyvRedis, 1h TTL"
      contains: "KeyvRedis"
  key_links:
    - from: "backend/src/throttler/throttler.module.ts"
      to: "nestjs-throttler-storage-redis + ioredis"
      via: "storage: new ThrottlerStorageRedisService(new Redis(REDIS_URL, { enableOfflineQueue: false }))"
      pattern: "ThrottlerStorageRedisService"
    - from: "backend/src/main.ts"
      to: "backend/src/throttler/throttler-exception.filter.ts"
      via: "app.useGlobalFilters(new ThrottlerExceptionFilter())"
      pattern: "ThrottlerExceptionFilter"
    - from: "backend/src/app.module.ts"
      to: "@nestjs/throttler ThrottlerGuard"
      via: "{ provide: APP_GUARD, useClass: ThrottlerGuard }"
      pattern: "APP_GUARD"
---

<objective>
Add Redis-backed caching and rate limiting on top of the proven skeleton: a dual-store CacheModule (L1 in-memory + L2 Redis), a Redis-backed ThrottlerModule (3 requests / 60s per client IP) wired as a global guard with a proxy-aware `getTracker`, and a custom `ThrottlerExceptionFilter` that returns the exact 429 body and `Retry-After` header. A `POST /rate-probe` endpoint demonstrates the limit end-to-end (the real POST /generate arrives in Phase 2).

Purpose: Establish the cross-cutting caching and rate-limiting infrastructure all later endpoints depend on. Counters live in Redis so they survive restarts; the proxy-aware tracker ensures limiting is per real client IP on Railway, not global.

Output: A globally-available CacheModule, a global Redis-backed rate limit guard, a custom 429 filter, and a probe endpoint proving 3/min/IP enforcement.

Implements API-04, RATE-01, RATE-02. Honors D-12 (dual Redis clients), D-13 (REDIS_URL), D-14 (1h cache TTL), D-15 ({ ttl:60000, limit:3 }), D-16 (trust proxy already set in 01-01 main.ts), D-17 (getTracker real IP), D-18 (exact 429 body + header). Incorporates RESEARCH findings: getTracker inline in forRoot (no class extension), custom ThrottlerExceptionFilter for D-18, ioredis enableOfflineQueue:false (Pitfall 7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-backend-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-backend-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-backend-infrastructure/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (01-03) creates:

**Modules / providers:**
- `CacheModule` configuration (backend/src/cache/cache.module.ts) — global, dual-store; exposes `CACHE_MANAGER` token for Phase 2
- `ThrottlerModule` configuration (backend/src/throttler/throttler.module.ts) — Redis-backed, inline getTracker
- `ThrottlerExceptionFilter` (backend/src/throttler/throttler-exception.filter.ts) — exact D-18 429 response
- Global `APP_GUARD` binding to `ThrottlerGuard` (in app.module.ts)
- `RateProbeController` (backend/src/health/rate-probe.controller.ts) — POST /rate-probe, demonstrates the limit

**Consumed from 01-01:** `app.set('trust proxy', 1)` in main.ts; ConfigService env `REDIS_URL`; AppModule.

**Env vars used:** `REDIS_URL` (already in EnvSchema from 01-01).

**Provided to Phase 2:** `CACHE_MANAGER` (cache reads/writes in GenerationService); the global throttler guard protects POST /generate automatically.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThrottlerModule (Redis-backed, proxy-aware) + ThrottlerExceptionFilter + global guard + rate-probe</name>
  <read_first>
    - .planning/phases/01-core-backend-infrastructure/01-RESEARCH.md (Pattern 6 ThrottlerModule + Redis + trust proxy + ThrottlerExceptionFilter; Pitfall 3 proxy IP; Pitfall 7 enableOfflineQueue; Pitfall 8 D-18 not automatic; new finding 3)
    - .planning/phases/01-core-backend-infrastructure/01-CONTEXT.md (D-12, D-13, D-15, D-16, D-17, D-18)
    - backend/src/main.ts (from 01-01 — trust proxy already set)
  </read_first>
  <files>backend/src/throttler/throttler.module.ts, backend/src/throttler/throttler-exception.filter.ts, backend/src/throttler/throttler-exception.filter.spec.ts, backend/src/health/rate-probe.controller.ts, backend/test/throttler.e2e-spec.ts, backend/src/main.ts, backend/src/app.module.ts</files>
  <behavior>
    - ThrottlerExceptionFilter.catch sets status 429, header Retry-After: 60, and JSON body { statusCode: 429, message: 'Too Many Requests', retryAfter: 60 } (unit test on a mocked ArgumentsHost/Response).
    - e2e: 3 sequential POST /rate-probe from the same IP return 200/201; the 4th returns 429 with the exact D-18 body and Retry-After header.
  </behavior>
  <action>
    Install `@nestjs/throttler@6.5.0`, `nestjs-throttler-storage-redis@0.5.1`, `ioredis@5.11.0` in `backend/`.

    Create `backend/src/throttler/throttler.module.ts` re-exporting a configured `ThrottlerModule.forRoot(...)` per RESEARCH Pattern 6 (D-15, D-17): `throttlers: [{ name: 'default', ttl: 60_000, limit: 3 }]`; `storage: new ThrottlerStorageRedisService(new Redis(process.env.REDIS_URL, { enableOfflineQueue: false }))` (D-12, D-13; Pitfall 7) — attach `.on('error', ...)` on the ioredis instance to avoid unhandled EventEmitter crashes; `getTracker: (req) => Promise.resolve(req.ips?.length > 0 ? req.ips[0] : req.ip)` (D-17 inline, no class extension — RESEARCH new finding 3). This ioredis client is isolated to the throttler (separate from the cache client — D-12).

    Create `backend/src/throttler/throttler-exception.filter.ts` per RESEARCH Pattern 6 / Pitfall 8 (D-18): `@Catch(ThrottlerException)` class implementing `ExceptionFilter`; `catch()` gets the Express response and calls `.status(429).header('Retry-After','60').json({ statusCode: 429, message: 'Too Many Requests', retryAfter: 60 })`. Create `throttler-exception.filter.spec.ts` unit-testing this with a mocked `ArgumentsHost` whose response records status/header/json calls.

    In `backend/src/app.module.ts`: import the configured ThrottlerModule and add provider `{ provide: APP_GUARD, useClass: ThrottlerGuard }` (global rate limiting — RATE-01).

    In `backend/src/main.ts`: register the filter globally with `app.useGlobalFilters(new ThrottlerExceptionFilter())` (D-18). Trust proxy is already set in 01-01 — do NOT duplicate or move it after listen (Anti-Pattern).

    Create `backend/src/health/rate-probe.controller.ts`: `@Controller('rate-probe')` with `@Post() probe()` returning `{ ok: true }` — exists solely to demonstrate the 3/min limit until POST /generate lands in Phase 2. Register it in app.module controllers.

    Create `backend/test/throttler.e2e-spec.ts`: boot the app with supertest, fire 4 POST /rate-probe with a fixed X-Forwarded-For, assert the first 3 succeed and the 4th returns 429 with body `{ statusCode: 429, message: 'Too Many Requests', retryAfter: 60 }` and a `Retry-After` header. (Requires a reachable Redis; if Redis is unavailable in CI, the unit spec on the filter still gates the exact body shape.)
  </action>
  <verify>
    <automated>cd backend && npm run build && npx jest src/throttler/throttler-exception.filter.spec.ts && grep -q "ttl: 60_000" src/throttler/throttler.module.ts && grep -q "limit: 3" src/throttler/throttler.module.ts && grep -q "getTracker" src/throttler/throttler.module.ts && grep -q "enableOfflineQueue: false" src/throttler/throttler.module.ts && grep -q "retryAfter: 60" src/throttler/throttler-exception.filter.ts && grep -q "APP_GUARD" src/app.module.ts && grep -q "useGlobalFilters(new ThrottlerExceptionFilter" src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/throttler/throttler-exception.filter.spec.ts` passes; the filter sets status 429, `Retry-After: 60` header, and body `{ statusCode: 429, message: 'Too Many Requests', retryAfter: 60 }`.
    - `backend/src/throttler/throttler.module.ts` contains `ttl: 60_000`, `limit: 3`, `getTracker`, `ThrottlerStorageRedisService`, `enableOfflineQueue: false` and uses `req.ips` for the tracker.
    - `backend/src/app.module.ts` contains `{ provide: APP_GUARD, useClass: ThrottlerGuard }`.
    - `backend/src/main.ts` contains `app.useGlobalFilters(new ThrottlerExceptionFilter())` and still sets `trust proxy` before `app.listen` (verify: `trust proxy` line precedes the `listen` call).
    - `cd backend && npm run build` exits 0.
    - (With Redis available) `npx jest test/throttler.e2e-spec.ts` shows the 4th request returns 429 with the exact body.
  </acceptance_criteria>
  <done>Redis-backed ThrottlerModule enforces 3/60s per real client IP; the custom filter returns the exact D-18 429 body + Retry-After; global guard and rate-probe endpoint wired; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-store CacheModule (L1 memory + L2 Redis)</name>
  <read_first>
    - .planning/phases/01-core-backend-infrastructure/01-RESEARCH.md (Pattern 7 CacheModule dual-store; Anti-Patterns — no CacheInterceptor on generation endpoint, single Redis client; Standard Stack cache versions)
    - .planning/phases/01-core-backend-infrastructure/01-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <files>backend/src/cache/cache.module.ts, backend/src/app.module.ts</files>
  <action>
    Install `@nestjs/cache-manager@3.1.2`, `cache-manager@7.2.8`, `@keyv/redis@5.1.6`, `keyv@5.6.0`, `cacheable@2.3.5` in `backend/`.

    Create `backend/src/cache/cache.module.ts` re-exporting a configured `CacheModule.registerAsync(...)` per RESEARCH Pattern 7 (D-12, D-14): `isGlobal: true`; `useFactory` returns `{ stores: [ new Keyv({ store: new KeyvCacheableMemory({ ttl: 60_000, lruSize: 1000 }) }), new KeyvRedis(process.env.REDIS_URL) ], ttl: 3_600_000 }` (1 hour, D-14). This Redis connection (@keyv/redis) is SEPARATE from the throttler's ioredis client (D-12 — do NOT share one client; Anti-Pattern).

    Register the configured CacheModule in `backend/src/app.module.ts` imports. Do NOT apply `CacheInterceptor` globally or on any endpoint (Anti-Pattern: POST body is not in the HTTP interceptor cache key; Phase 2 manages cache manually in GenerationService).
  </action>
  <verify>
    <automated>cd backend && npm run build && grep -q "KeyvRedis" src/cache/cache.module.ts && grep -q "KeyvCacheableMemory" src/cache/cache.module.ts && grep -q "isGlobal: true" src/cache/cache.module.ts && grep -q "3_600_000" src/cache/cache.module.ts && ! grep -rq "CacheInterceptor" src/</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/cache/cache.module.ts` contains `KeyvRedis`, `KeyvCacheableMemory`, `isGlobal: true`, and `ttl: 3_600_000` (1h, D-14).
    - The cache Redis client (`KeyvRedis`) is distinct from the throttler ioredis client (two separate constructions across the two module files — verify both files exist and neither imports the other's client).
    - No `CacheInterceptor` is registered anywhere in `backend/src` (`! grep -rq 'CacheInterceptor' backend/src`).
    - `backend/src/app.module.ts` imports the configured CacheModule.
    - `cd backend && npm run build` exits 0.
  </acceptance_criteria>
  <done>Global dual-store CacheModule (L1 memory + L2 Redis) with 1h TTL is wired; CACHE_MANAGER available for Phase 2; no HTTP CacheInterceptor applied; separate Redis client from throttler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway proxy → ThrottlerGuard | Client IP derived from X-Forwarded-For (req.ips[0]); spoofing here defeats per-IP limiting |
| App → Redis (throttler + cache) | Counters and cached values cross to Redis; separate logical clients |
| Excess requests → API | Rate limit is the only access control in Phase 1 (no auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Spoofing | X-Forwarded-For client IP | mitigate | `trust proxy: 1` (01-01) trusts only the first hop; getTracker uses `req.ips[0] ?? req.ip` so only Railway's proxy-set value is honored (D-16, D-17; Pitfall 3; ASVS V4) |
| T-01-10 | Denial of Service | Unbounded request rate | mitigate | ThrottlerModule { ttl:60000, limit:3 } per IP -> 429; global APP_GUARD (RATE-01, API-04) |
| T-01-11 | Denial of Service | Redis outage hangs requests | mitigate | ioredis `enableOfflineQueue: false` fails fast; `.on('error')` handler prevents process crash (Pitfall 7) |
| T-01-12 | Tampering | Redis key namespace collision | mitigate | Throttler (ioredis) and cache (@keyv/redis) use separate logical clients; Phase 2 cache keys prefixed `metagen:gen:` (D-12) |
| T-01-13 | Information Disclosure | 429 error body | accept | Body is intentionally exact { statusCode, message, retryAfter } (D-18) — no sensitive data; safe to expose (ASVS V7) |
| T-01-SC | Tampering | throttler/cache npm installs | mitigate | `@nestjs/throttler`, `nestjs-throttler-storage-redis`, `ioredis`, `@keyv/redis`, `keyv`, `cacheable`, `cache-manager` all [Approved] in RESEARCH audit; no postinstall network calls; no [ASSUMED]/[SUS] -> no checkpoint |
</threat_model>

<verification>
- `cd backend && npm run build` compiles with cache + throttler modules.
- `npx jest src/throttler` green (filter unit test; e2e when Redis available).
- Restart-survival check (manual/integration at phase end): with Redis running, exceed the limit, restart the process, confirm the counter window persists in Redis (Phase 1 success criterion #3).
- 429 body assertion matches D-18 exactly.
</verification>

<success_criteria>
- 4th request/60s/IP returns 429 with Retry-After header (API-04, RATE-01; Phase 1 success criterion #1).
- Rate limiting is Redis-backed and survives restart (RATE-02; Phase 1 success criterion #3).
- getTracker yields real client IP behind the proxy (RATE-02, D-17; Phase 1 success criterion #5 builds on trust proxy from 01-01).
- 429 body is exactly { statusCode: 429, message: 'Too Many Requests', retryAfter: 60 } (D-18).
- Dual-store CacheModule globally available with 1h TTL (D-12, D-14), separate Redis client from throttler.
</success_criteria>

<output>
Create `.planning/phases/01-core-backend-infrastructure/01-03-SUMMARY.md` when done.
</output>
